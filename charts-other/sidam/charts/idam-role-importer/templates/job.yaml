{{- if .Values.enabled }}
apiVersion: batch/v1
kind: Job
metadata:
  name: idam-role-importer
  labels:
    app.kubernetes.io/name: idam-role-importer
    app.kubernetes.io/instance: {{ .Release.Name }}-idam-role-importer
    app.kubernetes.io/managed-by: {{ .Release.Service }}
    helm.sh/chart: "{{ .Chart.Name }}-{{ .Chart.Version }}"
  annotations:
    helm.sh/hook: post-install,post-upgrade
    helm.sh/hook-weight: "5"
    helm.sh/hook-delete-policy: before-hook-creation
spec:
  template:
    metadata:
      name: idam-role-importer
      labels:
        app.kubernetes.io/name: idam-role-importer
    spec:
      containers:
        {{- $self := . -}}
        {{- range $index, $item := .Values.roles }}
        - name: idam-role-importer-{{ $index }}
          image: hmctspublic.azurecr.io/hmcts/idam/utils:latest
          command: [ /scripts/create-role.sh ]
          args:
            - {{ .id | quote }}
          env:
            - name: IDAM_API_URL
              value: http://idam-api
            - name: IDAM_ADMIN_USER
              value: {{ $self.Values.global.adminUser }}
            - name: IDAM_ADMIN_PASSWORD
              value: {{ $self.Values.global.adminPassword }}
          resources:
            requests:
              cpu: 125m
              memory: 8Mi
            limits:
              cpu: 250m
              memory: 16Mi
          imagePullPolicy: IfNotPresent
        {{- end }}
      restartPolicy: Never
{{- end }}