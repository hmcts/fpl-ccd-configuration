server:
  port: 4000

management:
  endpoints:
    web:
      base-path: /

spring:
  application:
    name: fpl-service

idam:
  s2s-auth:
    microservice: fpl_case_service

---

spring:
  profiles: integration-test

management:
  endpoints:
    enabled-by-default: false
