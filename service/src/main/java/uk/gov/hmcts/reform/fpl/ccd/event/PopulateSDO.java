package uk.gov.hmcts.reform.fpl.ccd.event;

import java.util.EnumSet;
import java.util.Set;
import org.springframework.stereotype.Component;
import uk.gov.hmcts.ccd.sdk.api.CCDConfig;
import uk.gov.hmcts.ccd.sdk.api.ConfigBuilder;
import uk.gov.hmcts.ccd.sdk.api.Permission;
import uk.gov.hmcts.reform.fpl.enums.State;
import uk.gov.hmcts.reform.fpl.model.CaseData;
import uk.gov.hmcts.reform.fpl.model.UserRole;

/**
 * Generated by ccd-definition-converter from CARE_SUPERVISION_EPO on migration; owned by this service team.
 *
 * <p>Event {@code populateSDO} for case type {@code CARE_SUPERVISION_EPO}.
 */
@Component
public class PopulateSDO implements CCDConfig<CaseData, State, UserRole> {
    /**
     * The CCD event ID.
     */
    public static final String POPULATE_SDO = "populateSDO";

    /**
     * Registers event {@code populateSDO}.
     *
     * @param builder the config builder
     */
    @Override
    public void configure(ConfigBuilder<CaseData, State, UserRole> builder) {
        var fields = builder.event(POPULATE_SDO)
            .forStateTransition(EnumSet.allOf(State.class), State.GATEKEEPING)
            .name("Populate standard directions")
            .displayOrder(14)
            .explicitGrants()
            .grant(Set.of(Permission.C, Permission.U), UserRole.CASEWORKER_PUBLICLAW_SYSTEMUPDATE)
            .fields();
        fields.page("1");
        fields.pageLabel(" ");
        fields.optional(CaseData::getAllParties);
        fields.optional(CaseData::getLocalAuthorityDirections);
        fields.optional(CaseData::getRespondentDirections);
        fields.optional(CaseData::getCafcassDirections);
        fields.optional(CaseData::getOtherPartiesDirections);
        fields.optional(CaseData::getCourtDirections);
    }
}
