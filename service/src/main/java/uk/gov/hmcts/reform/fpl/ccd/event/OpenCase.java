package uk.gov.hmcts.reform.fpl.ccd.event;

import org.springframework.stereotype.Component;
import uk.gov.hmcts.ccd.sdk.api.CCDConfig;
import uk.gov.hmcts.ccd.sdk.api.ConfigBuilder;
import uk.gov.hmcts.ccd.sdk.api.Permission;
import uk.gov.hmcts.reform.fpl.ccd.event.page.OpenCaseCheckSharingPage;
import uk.gov.hmcts.reform.fpl.ccd.event.page.OpenCaseProvideCaseNamePage;
import uk.gov.hmcts.reform.fpl.ccd.event.page.OpenCaseSelectOutsourcingLAPage;
import uk.gov.hmcts.reform.fpl.enums.State;
import uk.gov.hmcts.reform.fpl.model.CaseData;
import uk.gov.hmcts.reform.fpl.model.UserRole;

/**
 * Generated by ccd-definition-converter from CARE_SUPERVISION_EPO on migration; owned by this service team.
 *
 * <p>Event {@code openCase} for case type {@code CARE_SUPERVISION_EPO}.
 */
@Component
public class OpenCase implements CCDConfig<CaseData, State, UserRole> {
    /**
     * The CCD event ID.
     */
    public static final String OPEN_CASE = "openCase";

    /**
     * Registers event {@code openCase}.
     *
     * @param builder the config builder
     */
    @Override
    public void configure(ConfigBuilder<CaseData, State, UserRole> builder) {
        var fields = builder.event(OPEN_CASE)
            .initialState(State.OPEN)
            .name("Start application")
            .description("Create a new case – add a title")
            .displayOrder(1)
            .showSummary(false)
            .ttlIncrement(180)
            .explicitGrants()
            .grant(Permission.CR, UserRole.CASEWORKER_PUBLICLAW_SOLICITOR)
            .fields();
        OpenCaseSelectOutsourcingLAPage.apply(fields);
        OpenCaseProvideCaseNamePage.apply(fields);
        OpenCaseCheckSharingPage.apply(fields);
    }
}
