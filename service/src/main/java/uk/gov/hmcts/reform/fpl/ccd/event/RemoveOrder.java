package uk.gov.hmcts.reform.fpl.ccd.event;

import org.springframework.stereotype.Component;
import uk.gov.hmcts.ccd.sdk.api.CCDConfig;
import uk.gov.hmcts.ccd.sdk.api.ConfigBuilder;
import uk.gov.hmcts.ccd.sdk.api.Permission;
import uk.gov.hmcts.reform.fpl.ccd.event.page.RemoveOrderPage1;
import uk.gov.hmcts.reform.fpl.ccd.event.page.RemoveOrderRemoveApplicationFormPage;
import uk.gov.hmcts.reform.fpl.ccd.event.page.RemoveOrderRemoveOrderSummaryPage;
import uk.gov.hmcts.reform.fpl.enums.State;
import uk.gov.hmcts.reform.fpl.model.CaseData;
import uk.gov.hmcts.reform.fpl.model.UserRole;

/**
 * Generated by ccd-definition-converter from CARE_SUPERVISION_EPO on migration; owned by this service team.
 *
 * <p>Event {@code removeOrder} for case type {@code CARE_SUPERVISION_EPO}.
 */
@Component
public class RemoveOrder implements CCDConfig<CaseData, State, UserRole> {
    /**
     * The CCD event ID.
     */
    public static final String REMOVE_ORDER = "removeOrder";

    /**
     * Registers event {@code removeOrder}.
     *
     * @param builder the config builder
     */
    @Override
    public void configure(ConfigBuilder<CaseData, State, UserRole> builder) {
        var fields = builder.event(REMOVE_ORDER)
            .forStates(State.SUBMITTED, State.GATEKEEPING, State.CASE_MANAGEMENT, State.FINAL_HEARING, State.CLOSED)
            .name("Removal Tool")
            .description("Remove tool for removing an order, document or application from the case")
            .showSummary()
            .showEventNotes()
            .explicitGrants()
            .grant(Permission.CR, UserRole.CASEWORKER_PUBLICLAW_SUPERUSER)
            .fields();
        RemoveOrderPage1.apply(fields);
        RemoveOrderRemoveOrderSummaryPage.apply(fields);
        RemoveOrderRemoveApplicationFormPage.apply(fields);
    }
}
