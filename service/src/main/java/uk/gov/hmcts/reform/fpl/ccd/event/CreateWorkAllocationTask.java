package uk.gov.hmcts.reform.fpl.ccd.event;

import org.springframework.stereotype.Component;
import uk.gov.hmcts.ccd.sdk.api.CCDConfig;
import uk.gov.hmcts.ccd.sdk.api.ConfigBuilder;
import uk.gov.hmcts.ccd.sdk.api.Permission;
import uk.gov.hmcts.reform.fpl.enums.State;
import uk.gov.hmcts.reform.fpl.model.CaseData;
import uk.gov.hmcts.reform.fpl.model.UserRole;

/**
 * Generated by ccd-definition-converter from CARE_SUPERVISION_EPO on migration; owned by this service team.
 *
 * <p>Event {@code create-work-allocation-task} for case type {@code CARE_SUPERVISION_EPO}.
 */
@Component
public class CreateWorkAllocationTask implements CCDConfig<CaseData, State, UserRole> {
    /**
     * The CCD event ID.
     */
    public static final String CREATE_WORK_ALLOCATION_TASK = "create-work-allocation-task";

    /**
     * Registers event {@code create-work-allocation-task}.
     *
     * @param builder the config builder
     */
    @Override
    public void configure(ConfigBuilder<CaseData, State, UserRole> builder) {
        var fields = builder.event(CREATE_WORK_ALLOCATION_TASK)
            .forAllStates()
            .name("Create work allocation task")
            .description("Creates a task for work allocation")
            .displayOrder(1000)
            .showSummary(false)
            .endButtonLabel("Done")
            .publishToCamunda()
            .explicitGrants()
            .grant(Permission.CR, UserRole.CASEWORKER_PUBLICLAW_SYSTEMUPDATE)
            .fields();
        fields.page("1");
        fields.optionalNoSummary(CaseData::getLastCreatedWATask);
        fields.readonly(CaseData::getCourt)
                    .fieldShowCondition("lastCreatedWATask = \"DO_NOT_SHOW\"");
    }
}
