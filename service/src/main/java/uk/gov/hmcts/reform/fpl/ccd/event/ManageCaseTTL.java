package uk.gov.hmcts.reform.fpl.ccd.event;

import org.springframework.stereotype.Component;
import uk.gov.hmcts.ccd.sdk.api.CCDConfig;
import uk.gov.hmcts.ccd.sdk.api.ConfigBuilder;
import uk.gov.hmcts.ccd.sdk.api.Permission;
import uk.gov.hmcts.ccd.sdk.type.TTL;
import uk.gov.hmcts.reform.fpl.enums.State;
import uk.gov.hmcts.reform.fpl.model.CaseData;
import uk.gov.hmcts.reform.fpl.model.UserRole;

/**
 * Generated by ccd-definition-converter from CARE_SUPERVISION_EPO on migration; owned by this service team.
 *
 * <p>Event {@code manageCaseTTL} for case type {@code CARE_SUPERVISION_EPO}.
 */
@Component
public class ManageCaseTTL implements CCDConfig<CaseData, State, UserRole> {
    /**
     * The CCD event ID.
     */
    public static final String MANAGE_CASE_TTL = "manageCaseTTL";

    /**
     * Registers event {@code manageCaseTTL}.
     *
     * @param builder the config builder
     */
    @Override
    public void configure(ConfigBuilder<CaseData, State, UserRole> builder) {
        var fields = builder.event(MANAGE_CASE_TTL)
            .forAllStates()
            .name("Manage Case TTL")
            .displayOrder(15)
            .showSummary()
            .showEventNotes()
            .explicitGrants()
            .grant(Permission.CRU, UserRole.TTL_PROFILE)
            .grant(Permission.CRUD, UserRole.CASEWORKER_PUBLICLAW_SYSTEMUPDATE)
            .fields();
        fields.page("ManageCaseTTL");
        fields.complex(CaseData::getTTL)
                    .readonly(TTL::getSystemTTL)
                    .eventLabel("System TTL")
                    .optional(TTL::getOverrideTTL)
                    .eventLabel("Override TTL")
                    .optional(TTL::getSuspended)
                    .eventLabel("Suspend Deletion").done();
    }
}
