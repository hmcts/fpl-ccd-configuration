package uk.gov.hmcts.reform.fpl.ccd.event;

import org.springframework.stereotype.Component;
import uk.gov.hmcts.ccd.sdk.api.CCDConfig;
import uk.gov.hmcts.ccd.sdk.api.ConfigBuilder;
import uk.gov.hmcts.ccd.sdk.api.Permission;
import uk.gov.hmcts.reform.ccd.model.ChangeOrganisationRequest;
import uk.gov.hmcts.reform.ccd.model.Organisation;
import uk.gov.hmcts.reform.fpl.enums.State;
import uk.gov.hmcts.reform.fpl.model.CaseData;
import uk.gov.hmcts.reform.fpl.model.UserRole;

/**
 * Generated by ccd-definition-converter from CARE_SUPERVISION_EPO on migration; owned by this service team.
 *
 * <p>Event {@code updateRepresentation} for case type {@code CARE_SUPERVISION_EPO}.
 */
@Component
public class UpdateRepresentation implements CCDConfig<CaseData, State, UserRole> {
    /**
     * The CCD event ID.
     */
    public static final String UPDATE_REPRESENTATION = "updateRepresentation";

    /**
     * Registers event {@code updateRepresentation}.
     *
     * @param builder the config builder
     */
    @Override
    public void configure(ConfigBuilder<CaseData, State, UserRole> builder) {
        var fields = builder.event(UPDATE_REPRESENTATION)
            .forAllStates()
            .name("Update representation")
            .description("Update respondent representation via NoC")
            .displayOrder(73)
            .showSummary(false)
            .explicitGrants()
            .grant(Permission.CRUD, UserRole.CASEWORKER_PUBLICLAW_SYSTEMUPDATE)
            .fields();
        fields.page("SingleFormPageWithComplex");
        fields.pageLabel("Change Organisation Request");
        fields.complex(CaseData::getChangeOrganisationRequestField, false)
                    .complex(ChangeOrganisationRequest::getOrganisationToAdd)
                    .optional(Organisation::getOrganisationID).done()
                    .complex(ChangeOrganisationRequest::getOrganisationToAdd)
                    .optional(Organisation::getOrganisationName).done()
                    .complex(ChangeOrganisationRequest::getOrganisationToRemove)
                    .optional(Organisation::getOrganisationID).done()
                    .complex(ChangeOrganisationRequest::getOrganisationToRemove)
                    .optional(Organisation::getOrganisationName).done()
                    .optional(ChangeOrganisationRequest::getCaseRoleId)
                    .optional(ChangeOrganisationRequest::getRequestTimestamp)
                    .optional(ChangeOrganisationRequest::getApprovalStatus).done();
    }
}
