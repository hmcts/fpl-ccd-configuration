package uk.gov.hmcts.reform.fpl.ccd.event;

import org.springframework.stereotype.Component;
import uk.gov.hmcts.ccd.sdk.api.CCDConfig;
import uk.gov.hmcts.ccd.sdk.api.ConfigBuilder;
import uk.gov.hmcts.ccd.sdk.api.Permission;
import uk.gov.hmcts.reform.fpl.enums.State;
import uk.gov.hmcts.reform.fpl.model.CaseData;
import uk.gov.hmcts.reform.fpl.model.UserRole;

/**
 * Generated by ccd-definition-converter from CARE_SUPERVISION_EPO on migration; owned by this service team.
 *
 * <p>Event {@code removeManagingOrganisation} for case type {@code CARE_SUPERVISION_EPO}.
 */
@Component
public class RemoveManagingOrganisation implements CCDConfig<CaseData, State, UserRole> {
    /**
     * The CCD event ID.
     */
    public static final String REMOVE_MANAGING_ORGANISATION = "removeManagingOrganisation";

    /**
     * Registers event {@code removeManagingOrganisation}.
     *
     * @param builder the config builder
     */
    @Override
    public void configure(ConfigBuilder<CaseData, State, UserRole> builder) {
        var fields = builder.event(REMOVE_MANAGING_ORGANISATION)
            .forStates(State.SUBMITTED, State.GATEKEEPING, State.CASE_MANAGEMENT, State.FINAL_HEARING)
            .name("Remove managing organisation")
            .description("Removes managing organisation")
            .displayOrder(14)
            .showCondition("outsourcingPolicy != \"\" AND outsourcingPolicy.Organisation.OrganisationID = \"*\"")
            .showSummary()
            .explicitGrants()
            .grant(Permission.CR, UserRole.LASOLICITOR)
            .fields();
        fields.page("Confirmation");
        fields.readonlyNoSummary(CaseData::getManagingOrganisationRemovalInfo);
        fields.readonly(CaseData::getManagingOrganisationName);
        fields.readonlyNoSummary(CaseData::getManagingOrganisationRemovalConfirmation);
    }
}
