buildscript {
  repositories {
    jcenter()
  }
  configurations.maybeCreate('pitest')
  dependencies {
    classpath group: 'info.solidsoft.gradle.pitest', name: 'gradle-pitest-plugin', version: '1.3.0'
    pitest group: 'org.pitest', name: 'pitest-junit5-plugin', version: '0.8'
  }
}
