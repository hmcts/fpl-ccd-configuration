version: 1.0

steps:
  - id: build-image
    build: -t {{.Run.Registry}}/{{CI_IMAGE_TAG}} .
    keep: true
  - id: push-image
    push:
      - {{.Run.Registry}}/{{CI_IMAGE_TAG}}
    when:
      - build-image