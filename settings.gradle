pluginManagement {
  repositories {
    maven {
      url '../../build/repo'
    }
    gradlePluginPortal()
  }
}

rootProject.name = 'FPL'

include 'service'
includeBuild ('../../ccd-sdk-types') {
  dependencySubstitution {
    substitute module('ccd-sdk-types:types') with project(':')
  }
}
